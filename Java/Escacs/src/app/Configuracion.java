package app;
/*
Java GUI 2: Configuration Class (Configuracion)

A simple class that holds the current theme setting of the application. It uses a static variable to store the theme, 
which can be modified from different parts of the application.

    Configuracion: Contains a static field tema that holds the current theme, which is accessible and modifiable by other classes.

*/
public class Configuracion {
    public static String tema = "Clasico";
}
